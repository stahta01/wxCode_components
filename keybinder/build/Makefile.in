# =========================================================================
#     This makefile was generated by
#     Bakefile 0.1.4 (http://bakefile.sourceforge.net)
#     Do not modify, all changes will be overwritten!
# =========================================================================


@MAKE_SET@

prefix = @prefix@
exec_prefix = @exec_prefix@
INSTALL = @INSTALL@
LIBEXT = @LIBEXT@
LIBPREFIX = @LIBPREFIX@
EXEEXT = @EXEEXT@
RESCOMP = @RESCOMP@
SETFILE = @SETFILE@
NM = @NM@
INSTALL_DATA = @INSTALL_DATA@
INSTALL_DIR = @INSTALL_DIR@
srcdir = @srcdir@
top_builddir = @top_builddir@
libdir = @libdir@
includedir = @includedir@
LDFLAGS_GUI = @LDFLAGS_GUI@
AR = @AR@
RANLIB = @RANLIB@
CXX = @CXX@
CXXFLAGS = @CXXFLAGS@
CPPFLAGS = @CPPFLAGS@
LDFLAGS = @LDFLAGS@

### Variables: ###

DESTDIR = 
KEYBINDER_CXXFLAGS = -I../include `wx-config --cxxflags` -Wall $(CPPFLAGS) \
	$(CXXFLAGS)
KEYBINDER_OBJECTS =  \
	keybinder_keybinder.o \
	keybinder_menuutils.o
MINIMAL_CXXFLAGS = -I../include `wx-config --cxxflags` -Wall $(CPPFLAGS) \
	$(CXXFLAGS)
MINIMAL_OBJECTS =  \
	minimal_minimal.o \
	$(__minimal___win32rc)

### Conditionally set variables: ###

@COND_DEPS_TRACKING_1@CXXC = $(top_builddir)./bk-deps $(CXX)
@COND_DEPS_TRACKING_0@CXXC = $(CXX)
@COND_BUILD_DEBUG_UNICODE_1@WXLIBPOSTFIX = ud
@COND_BUILD_DEBUG_UNICODE_0@WXLIBPOSTFIX = d
@COND_BUILD_RELEASE_UNICODE_1@WXLIBPOSTFIX = u
@COND_PLATFORM_MAC_1@__minimal___mac_setfilecmd = \
@COND_PLATFORM_MAC_1@	$(SETFILE) -t APPL minimal$(EXEEXT)
COND_PLATFORM_OS2_1___minimal___os2_emxbindcmd = $(NM) minimal$(EXEEXT) | if \
	grep -q pmwin.763 ; then emxbind -ep minimal$(EXEEXT) ; fi
@COND_PLATFORM_OS2_1@__minimal___os2_emxbindcmd = $(COND_PLATFORM_OS2_1___minimal___os2_emxbindcmd)
@COND_PLATFORM_WIN32_1@__minimal___win32rc = minimal_minimal_rc.o


### Targets: ###

all: ../lib/$(LIBPREFIX)keybinder$(WXLIBPOSTFIX)$(LIBEXT) ../sample/minimal$(EXEEXT)

install: all install-wxheaders install-lib

uninstall: 
	rm -f $(includedir)/wx/keybinder.h $(includedir)/wx/menuutils.h
	rm -f $(libdir)/libkeybinder*.a

install-strip: install

clean: 
	rm -rf ./.deps ./.pch
	rm -f ./*.o
	rm -f ../lib/$(LIBPREFIX)keybinder$(WXLIBPOSTFIX)$(LIBEXT)
	rm -f ../sample/minimal$(EXEEXT)

distclean: clean
	rm -f config.cache config.log config.status bk-deps bk-make-pch shared-ld-sh Makefile

tarball: 
	( cd .. && tar -cvzf keybinder.tar.gz --exclude=*.pdb --exclude=*.log --exclude=*.o* * )

zip: 
	( cd .. && zip -r9 keybinder.zip *  -x *.pdb -x *.log -x *.o* )

cleanbuilddirs: 
	rm -rf msvc
	rm -rf watcom
	rm -rf borland
	rm -rf mingw

cleanall: clean cleanbuilddirs
	rm -r ..\*.log
	rm -r ..\*.lib
	rm -r ..\*.a
	rm -r ..\*.exe

docs: 
	( cd ../docs && doxygen )

cleandocs: 
	rm -rf ../docs/html

install-wxheaders: 
	$(INSTALL_DIR) $(DESTDIR)$(includedir)/wx
	(cd $(srcdir) ; $(INSTALL_DATA)  ../include/wx/*.h $(DESTDIR)$(includedir)/wx)

install-lib: 
	$(INSTALL_DIR) $(DESTDIR)$(libdir)
	(cd $(srcdir) ; $(INSTALL_DATA)  ../lib/*.a $(DESTDIR)$(libdir))

../lib/$(LIBPREFIX)keybinder$(WXLIBPOSTFIX)$(LIBEXT): $(KEYBINDER_OBJECTS)
	rm -f $@
	$(AR) rcu $@ $(KEYBINDER_OBJECTS)
	$(RANLIB) $@

../sample/minimal$(EXEEXT): $(MINIMAL_OBJECTS) $(__minimal___win32rc) ../lib/$(LIBPREFIX)keybinder$(WXLIBPOSTFIX)$(LIBEXT)
	$(CXX) -o $@ $(MINIMAL_OBJECTS) $(LDFLAGS)  $(LDFLAGS_GUI) -L../lib `wx-config --libs`  ../lib/$(LIBPREFIX)keybinder$(WXLIBPOSTFIX)$(LIBEXT)
	
	$(__minimal___mac_setfilecmd)
	$(__minimal___os2_emxbindcmd)

keybinder_keybinder.o: $(srcdir)/../src/keybinder.cpp
	$(CXXC) -c -o $@ $(KEYBINDER_CXXFLAGS) $<

keybinder_menuutils.o: $(srcdir)/../src/menuutils.cpp
	$(CXXC) -c -o $@ $(KEYBINDER_CXXFLAGS) $<

minimal_minimal.o: $(srcdir)/../sample/minimal.cpp
	$(CXXC) -c -o $@ $(MINIMAL_CXXFLAGS) $<

minimal_minimal_rc.o: $(srcdir)/../sample/minimal.rc
	$(RESCOMP) -i$< -o$@  --include-dir ../include --include-dir ../sample


# Include dependency info, if present:
@IF_GNU_MAKE@-include .deps/*.d

.PHONY: all install uninstall clean distclean
