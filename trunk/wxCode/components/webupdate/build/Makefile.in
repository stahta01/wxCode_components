# =========================================================================
#     This makefile was generated by
#     Bakefile 0.1.8 (http://bakefile.sourceforge.net)
#     Do not modify, all changes will be overwritten!
# =========================================================================


@MAKE_SET@

prefix = @prefix@
exec_prefix = @exec_prefix@
INSTALL = @INSTALL@
LIBEXT = @LIBEXT@
LIBPREFIX = @LIBPREFIX@
EXEEXT = @EXEEXT@
RESCOMP = @RESCOMP@
SETFILE = @SETFILE@
NM = @NM@
INSTALL_DATA = @INSTALL_DATA@
INSTALL_DIR = @INSTALL_DIR@
srcdir = @srcdir@
top_builddir = @top_builddir@
libdir = @libdir@
includedir = @includedir@
LDFLAGS_GUI = @LDFLAGS_GUI@
AR = @AR@
RANLIB = @RANLIB@
CXX = @CXX@
CXXFLAGS = @CXXFLAGS@
CPPFLAGS = @CPPFLAGS@
LDFLAGS = @LDFLAGS@
WX_CXXFLAGS = @WX_CXXFLAGS@
WX_LIBS = @WX_LIBS@

### Variables: ###

DESTDIR = 
WEBUPDATE_CXXFLAGS =  -I../include  $(WX_CXXFLAGS) $(CPPFLAGS) $(CXXFLAGS)
WEBUPDATE_OBJECTS =  \
	webupdate_webupdate.o \
	webupdate_webupdatedlg.o \
	webupdate_checkedlistctrl.o
MINIMAL_CXXFLAGS =  -I../include  $(WX_CXXFLAGS) $(CPPFLAGS) $(CXXFLAGS)
MINIMAL_OBJECTS =  \
	minimal_minimal.o \
	$(__minimal___win32rc)

### Conditionally set variables: ###

@COND_DEPS_TRACKING_1@CXXC = $(top_builddir)./bk-deps $(CXX)
@COND_DEPS_TRACKING_0@CXXC = $(CXX)
@COND_PLATFORM_MAC_1@__minimal___mac_setfilecmd = \
@COND_PLATFORM_MAC_1@	$(SETFILE) -t APPL minimal$(EXEEXT)
COND_PLATFORM_OS2_1___minimal___os2_emxbindcmd = $(NM) minimal$(EXEEXT) | if \
	grep -q pmwin.763 ; then emxbind -ep minimal$(EXEEXT) ; fi
@COND_PLATFORM_OS2_1@__minimal___os2_emxbindcmd = $(COND_PLATFORM_OS2_1___minimal___os2_emxbindcmd)
@COND_PLATFORM_WIN32_1@__minimal___win32rc = minimal_minimal_rc.o

### Targets: ###

all: ../lib/$(LIBPREFIX)webupdate$(LIBEXT) ../sample/minimal$(EXEEXT)

install: all install-wxheaders install-lib

uninstall: 
	rm -f $(includedir)/wx/webupdate.h
	rm -f $(libdir)/libwebupdate*.a

install-strip: install

clean: 
	rm -rf ./.deps ./.pch
	rm -f ./*.o
	rm -f ../lib/$(LIBPREFIX)webupdate$(LIBEXT)
	rm -f ../sample/minimal$(EXEEXT)

distclean: clean
	rm -f config.cache config.log config.status bk-deps bk-make-pch shared-ld-sh Makefile

tarball: 
	( cd .. && tar -cvzf ../webupdate.tar.gz --exclude=*.pdb --exclude=*.log --exclude=*.o* * )

zip: 
	( cd .. && zip -r9 ../webupdate.zip *  -x *.pdb -x *.log -x *.o* )

cleanbuilddirs: 
	rm -rf msvc
	rm -rf watcom
	rm -rf borland
	rm -rf mingw
	rm -rf msvc6prj

cleanall: clean cleanbuilddirs
	rm -r ..\*.log
	rm -r ..\*.lib
	rm -r ..\*.a
	rm -r ..\*.exe

docs: 
	( cd ../docs && doxygen )

cleandocs: 
	rm -rf ../docs/html

install-wxheaders: 
	$(INSTALL_DIR) $(DESTDIR)$(includedir)/wx
	(cd $(srcdir) ; $(INSTALL_DATA)  ../include/wx/*.h $(DESTDIR)$(includedir)/wx)

install-lib: 
	$(INSTALL_DIR) $(DESTDIR)$(libdir)
	(cd $(srcdir) ; $(INSTALL_DATA)  ../lib/*.a $(DESTDIR)$(libdir))

../lib/$(LIBPREFIX)webupdate$(LIBEXT): $(WEBUPDATE_OBJECTS)
	rm -f $@
	$(AR) rcu $@ $(WEBUPDATE_OBJECTS)
	$(RANLIB) $@

../sample/minimal$(EXEEXT): $(MINIMAL_OBJECTS) $(__minimal___win32rc) ../lib/$(LIBPREFIX)webupdate$(LIBEXT)
	$(CXX) -o $@ $(MINIMAL_OBJECTS) -L../lib $(LDFLAGS) -L../lib $(LDFLAGS_GUI)  $(WX_LIBS) -lwebupdate
	
	$(__minimal___mac_setfilecmd)
	$(__minimal___os2_emxbindcmd)

webupdate_webupdate.o: $(srcdir)/../src/webupdate.cpp
	$(CXXC) -c -o $@ $(WEBUPDATE_CXXFLAGS) $(srcdir)/../src/webupdate.cpp

webupdate_webupdatedlg.o: $(srcdir)/../src/webupdatedlg.cpp
	$(CXXC) -c -o $@ $(WEBUPDATE_CXXFLAGS) $(srcdir)/../src/webupdatedlg.cpp

webupdate_checkedlistctrl.o: $(srcdir)/../src/checkedlistctrl.cpp
	$(CXXC) -c -o $@ $(WEBUPDATE_CXXFLAGS) $(srcdir)/../src/checkedlistctrl.cpp

minimal_minimal.o: $(srcdir)/../sample/minimal.cpp
	$(CXXC) -c -o $@ $(MINIMAL_CXXFLAGS) $(srcdir)/../sample/minimal.cpp

minimal_minimal_rc.o: $(srcdir)/../sample/minimal.rc
	$(RESCOMP) -i$< -o$@ --include-dir ../include --include-dir ../sample


# Include dependency info, if present:
@IF_GNU_MAKE@-include .deps/*.d

.PHONY: all install uninstall clean distclean
