fonts:
	include fonts, so that the need to load it from file 
	is not longer necessary
	or
	include an appropriate search path for fonts: 
	(see LIB_DIR in plConfig.h)

lib:
	create library

docu:
	lot of work

