# =========================================================================
#     This makefile was generated by
#     Bakefile 0.1.9 (http://bakefile.sourceforge.net)
#     Do not modify, all changes will be overwritten!
# =========================================================================

.autodepend

!ifndef BCCDIR
!ifndef MAKEDIR
!error Your Borland compiler does not define MAKEDIR. Please define the BCCDIR variable, e.g. BCCDIR=d:\bc4
!endif
BCCDIR = $(MAKEDIR)\..
!endif



# -------------------------------------------------------------------------
# These are configurable options:
# -------------------------------------------------------------------------

# C compiler 
!ifndef CC
CC = bcc32
!endif

# C++ compiler 
!ifndef CXX
CXX = bcc32
!endif

# Standard flags for CC 
!ifndef CFLAGS
CFLAGS = 
!endif

# Standard flags for C++ 
!ifndef CXXFLAGS
CXXFLAGS = 
!endif

# Standard preprocessor flags (common for CC and CXX) 
!ifndef CPPFLAGS
CPPFLAGS = -a8 -g0
!endif

# Standard linker flags 
!ifndef LDFLAGS
LDFLAGS = 
!endif

#  
!ifndef BUILD
BUILD = 
!endif

#  
!ifndef SHARED
SHARED = 
!endif

#  
!ifndef UNICODE
UNICODE = 
!endif

#  
!ifndef ICONV_DIR
ICONV_DIR = 
!endif



# -------------------------------------------------------------------------
# Do not modify the rest of this file!
# -------------------------------------------------------------------------

### Variables: ###

MAKEARGS = -DCC="$(CC)" -DCXX="$(CXX)" -DCFLAGS="$(CFLAGS)" \
	-DCXXFLAGS="$(CXXFLAGS)" -DCPPFLAGS="$(CPPFLAGS)" -DLDFLAGS="$(LDFLAGS)" \
	-DBUILD="$(BUILD)" -DSHARED="$(SHARED)" -DUNICODE="$(UNICODE)" \
	-DICONV_DIR="$(ICONV_DIR)"

### Conditionally set variables: ###



### Targets: ###

all: libiconv libxml2

clean: 
	-if exist .\*.obj del .\*.obj
	-if exist .\*.res del .\*.res
	-if exist .\*.csm del .\*.csm
	@echo cd ..\libiconv\build >libiconv.bat
	@echo $(MAKE) -f makefile.bcc $(MAKEARGS) clean >>libiconv.bat
	call libiconv.bat
	@del libiconv.bat
	@echo cd ..\libxml2\build >libxml2.bat
	@echo $(MAKE) -f makefile.bcc $(MAKEARGS) clean >>libxml2.bat
	call libxml2.bat
	@del libxml2.bat

BUILD: 

SHARED: 

UNICODE: 

ICONV_DIR: 

libiconv: 
	@echo cd ..\libiconv\build >libiconv.bat
	@echo $(MAKE) -f makefile.bcc $(MAKEARGS)  >>libiconv.bat
	call libiconv.bat
	@del libiconv.bat

libxml2: 
	@echo cd ..\libxml2\build >libxml2.bat
	@echo $(MAKE) -f makefile.bcc $(MAKEARGS) libonly >>libxml2.bat
	call libxml2.bat
	@del libxml2.bat

