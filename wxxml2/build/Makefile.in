# =========================================================================
#     This makefile was generated by
#     Bakefile 0.1.4 (http://bakefile.sourceforge.net)
#     Do not modify, all changes will be overwritten!
# =========================================================================


@MAKE_SET@

prefix = @prefix@
exec_prefix = @exec_prefix@
INSTALL = @INSTALL@
LIBEXT = @LIBEXT@
LIBPREFIX = @LIBPREFIX@
EXEEXT = @EXEEXT@
SETFILE = @SETFILE@
NM = @NM@
INSTALL_DATA = @INSTALL_DATA@
INSTALL_DIR = @INSTALL_DIR@
srcdir = @srcdir@
top_builddir = @top_builddir@
libdir = @libdir@
includedir = @includedir@
LDFLAGS_GUI = @LDFLAGS_GUI@
AR = @AR@
RANLIB = @RANLIB@
CXX = @CXX@
CXXFLAGS = @CXXFLAGS@
CPPFLAGS = @CPPFLAGS@
LDFLAGS = @LDFLAGS@
LIBXML2_DIR = @LIBXML2_DIR@
ICONV_DIR = @ICONV_DIR@

### Variables: ###

DESTDIR = 
WXXML2_CXXFLAGS = -I../include -I$(LIBXML2_DIR)/include -I$(ICONV_DIR)/include \
	`wx-config --cxxflags` -Wall $(CPPFLAGS) $(CXXFLAGS)
WXXML2_OBJECTS =  \
	wxxml2_xml2.o \
	wxxml2_dtd.o
MINIMAL_CXXFLAGS = -I../include -I$(LIBXML2_DIR)/include \
	-I$(ICONV_DIR)/include `wx-config --cxxflags` -Wall $(CPPFLAGS) $(CXXFLAGS)
MINIMAL_OBJECTS =  \
	minimal_minimal.o

### Conditionally set variables: ###

@COND_DEPS_TRACKING_1@CXXC = $(top_builddir)./bk-deps $(CXX)
@COND_DEPS_TRACKING_0@CXXC = $(CXX)
@COND_BUILD_DEBUG_UNICODE_1@WXLIBPOSTFIX = ud
@COND_BUILD_DEBUG_UNICODE_0@WXLIBPOSTFIX = d
@COND_BUILD_RELEASE_UNICODE_1@WXLIBPOSTFIX = u
@COND_PLATFORM_MAC_1@__minimal___mac_setfilecmd = \
@COND_PLATFORM_MAC_1@	$(SETFILE) -t APPL minimal$(EXEEXT)
COND_PLATFORM_OS2_1___minimal___os2_emxbindcmd = $(NM) minimal$(EXEEXT) | if \
	grep -q pmwin.763 ; then emxbind -ep minimal$(EXEEXT) ; fi
@COND_PLATFORM_OS2_1@__minimal___os2_emxbindcmd = $(COND_PLATFORM_OS2_1___minimal___os2_emxbindcmd)

### Targets: ###

all: ../lib/$(LIBPREFIX)wxxml2$(WXLIBPOSTFIX)$(LIBEXT) ../sample/minimal$(EXEEXT)

install: all install-wxheaders install-lib

uninstall: 
	rm -f $(includedir)/wx/wxxml2.h $(includedir)/wx/dtd.h
	rm -f $(libdir)/libwxxml2*.a

install-strip: install

clean: 
	rm -rf ./.deps ./.pch
	rm -f ./*.o
	rm -f ../lib/$(LIBPREFIX)wxxml2$(WXLIBPOSTFIX)$(LIBEXT)
	rm -f ../sample/minimal$(EXEEXT)

distclean: clean
	rm -f config.cache config.log config.status bk-deps bk-make-pch shared-ld-sh Makefile

tarball: 
	( cd .. && tar -cvzf wxxml2.tar.gz --exclude=*.pdb --exclude=*.log --exclude=*.o* * )

zip: 
	( cd .. && zip -r9 wxxml2.zip *  -x *.pdb -x *.log -x *.o* )

cleanbuilddirs: 
	rm -rf msvc
	rm -rf watcom
	rm -rf borland
	rm -rf mingw

cleanall: clean cleanbuilddirs
	rm -r ..\*.log
	rm -r ..\*.lib
	rm -r ..\*.a
	rm -r ..\*.exe

docs: 
	( cd ../docs && doxygen )

cleandocs: 
	rm -rf ../docs/html

install-wxheaders: 
	$(INSTALL_DIR) $(DESTDIR)$(includedir)/wx
	(cd $(srcdir) ; $(INSTALL_DATA)  ../include/wx/*.h $(DESTDIR)$(includedir)/wx)

install-lib: 
	$(INSTALL_DIR) $(DESTDIR)$(libdir)
	(cd $(srcdir) ; $(INSTALL_DATA)  ../lib/*.a $(DESTDIR)$(libdir))

../lib/$(LIBPREFIX)wxxml2$(WXLIBPOSTFIX)$(LIBEXT): $(WXXML2_OBJECTS)
	rm -f $@
	$(AR) rcu $@ $(WXXML2_OBJECTS)
	$(RANLIB) $@

../sample/minimal$(EXEEXT): $(MINIMAL_OBJECTS) ../lib/$(LIBPREFIX)wxxml2$(WXLIBPOSTFIX)$(LIBEXT)
	$(CXX) -o $@ $(MINIMAL_OBJECTS) $(LDFLAGS)  -L../lib $(LDFLAGS_GUI) -L$(LIBXML2_DIR)/lib -L$(ICONV_DIR)/lib `wx-config --libs`  ../lib/$(LIBPREFIX)wxxml2$(WXLIBPOSTFIX)$(LIBEXT) -lxml2 -liconv
	
	$(__minimal___mac_setfilecmd)
	$(__minimal___os2_emxbindcmd)

wxxml2_xml2.o: $(srcdir)/../src/xml2.cpp
	$(CXXC) -c -o $@ $(WXXML2_CXXFLAGS) $<

wxxml2_dtd.o: $(srcdir)/../src/dtd.cpp
	$(CXXC) -c -o $@ $(WXXML2_CXXFLAGS) $<

minimal_minimal.o: $(srcdir)/../sample/minimal.cpp
	$(CXXC) -c -o $@ $(MINIMAL_CXXFLAGS) $<


# Include dependency info, if present:
@IF_GNU_MAKE@-include .deps/*.d

.PHONY: all install uninstall clean distclean
